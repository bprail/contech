RemSync - Remove Sync was a prototype backend that demonstrated graph
modification through identifying sequences of critical sections
that had no intervening contexts.

This backend is not up to date and is deprecated.
